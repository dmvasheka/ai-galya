{
  "name": "numerology-forecast",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @app/api dev\" \"pnpm --filter @app/web dev\"",
    "build": "pnpm -r build",
    "start": "pnpm --filter @app/api start & pnpm --filter @app/web start",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
